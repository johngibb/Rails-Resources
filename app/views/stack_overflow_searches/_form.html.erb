<%= form_for(@stack_overflow_search) do |f| %>
  <% if @stack_overflow_search.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stack_overflow_search.errors.count, "error") %> prohibited this stack_overflow_search from being saved:</h2>

      <ul>
      <% @stack_overflow_search.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :prefix %><br />
    <%= f.text_field :prefix %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
